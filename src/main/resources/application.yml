spring :
  application :
    name : movie-api
  datasource :
    username : root
    password : secret
    url : jdbc:mysql://localhost:3306/movies
    driver-class-name : com.mysql.cj.jdbc.Driver
  jpa :
    hibernate :
      ddl-auto : update
    show-sql : true
    properties :
      hibernate :
        format-sql : true
# configured path
projects :
  poster : posters/